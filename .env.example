# Zeptomail API Configuration (Contact Form)
# Get your API key from: https://www.zoho.com/zeptomail/
# Required for contact form email delivery
# Note: Key contains a space, so quotes are required
# For Vercel: Add to Project Settings → Environment Variables
ZEPTOMAIL_API_KEY='your_zeptomail_api_key_here'

# Contact Form Email Configuration
# CONTACT_EMAIL_TO: Where form submissions are sent (your email)
# CONTACT_EMAIL_FROM: Sender address (must be verified in Zeptomail)
# CONTACT_EMAIL_FROM_NAME: Optional sender name (defaults to "Portfolio Contact Form")
CONTACT_EMAIL_TO=your_email@example.com
CONTACT_EMAIL_FROM=noreply@yourdomain.com
CONTACT_EMAIL_FROM_NAME='Portfolio Contact Form'

# Upstash Redis (Rate Limiting)
# Auto-injected by the Vercel Marketplace Upstash integration (KV_* naming).
# Only needed locally if testing Redis rate limiting (otherwise in-memory fallback is used).
KV_REST_API_URL=your_upstash_redis_rest_url_here
KV_REST_API_TOKEN=your_upstash_redis_rest_token_here
